from __future__ import annotations

from pathlib import Path


def parse_filing_placeholder(path: Path) -> dict:
    """Phase 4 placeholder for SEC/SEBI filing parsing and diffing."""
    return {"path": str(path), "status": "not_implemented"}
